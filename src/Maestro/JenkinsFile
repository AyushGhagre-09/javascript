node {
    stage('Cleanup') {
        stage ('Clean Workspace') { 
    cleanWs notPaths: 'src/Maestro/node_modules/**' 
}

    }
    
  stage('Checkout and Install') {
    dir('src/Maestro') {
            checkout scm
            bat 'yarn'
       }
    }
}
