node {
  
  
  stage('Cleanup') {
    cleanWs(deleteDirs: true, patterns: [
    [pattern: '**/.git/**', type: 'EXCLUDE'],
    [pattern: 'src/Maestro/node_modules/**', type: 'INCLUDE'],  // Include node_modules
    [pattern: 'src/Maestro/**', type: 'EXCLUDE'],  // Exclude everything in src/Maestro
    [pattern: 'src/Maestro/', type: 'EXCLUDE']  // Ensure src/Maestro itself is excluded
])


    
    
   
  }

  stage('Checkout and Install') {
   // sleep 30
  //checkout scm
   
   
//     dir('src/Maestro'){
//     bat 'yarn'
//   }
 }
}
