node {
   stage('Cleanup') {
    cleanWs(
        deleteDirs: true,
        patterns: [
            [pattern: '**/node_modules/**', type: 'EXCLUDE']
        ]
    )
}

    
// stage('Checkout and Install') {
  //  dir('src/Maestro') {
     //       checkout scm
      //   bat 'yarn'
      // }
  //  }
}
